# Topilot 交互设计规范

## 1. 设计定位
Topilot 当前没有自研 Web 前端或桌面 GUI，用户界面完全依托 Telegram 原生聊天窗口完成。因此，设计规范的核心不是颜色和像素级视觉稿，而是消息结构、按钮布局、信息密度、状态表达和移动端可读性。

## 2. 交互载体约束
1. 主交互载体固定为 Telegram 文本消息和内联按钮。
2. 当前不设计表单页、设置页、独立控制台页面或自定义主题。
3. 若后续新增 Web UI，必须先更新 `designer.md`，再开始界面开发。

## 3. 文案与视觉风格

### 3.1 文案风格
1. 统一使用简洁、专业、面向执行的中文文案。
2. 状态提示优先使用“已完成 / 进行中 / 执行失败 / 未授权 / 当前模型”等明确词汇。
3. 禁止使用过度营销化语气或与开发工具定位不符的娱乐化文案。

### 3.2 状态表达
1. 当前会话使用 `⭐` 标记。
2. 运行中会话使用 `🟢` 标记，空闲会话使用 `⚪` 标记。
3. 当前选中模型使用 `✓` 标记。
4. 状态表达不能只依赖图标，文字说明必须同步存在。

## 4. 布局规则

### 4.1 对话消息布局
1. 正常对话采用“双消息流”展示：一条过程消息、一条回复消息。
2. 过程消息显示最近若干条工具调用和执行进展；回复消息显示 AI 的持续生成内容。
3. 会话接管后的实时历史展示采用“单消息反复刷新”模式，避免刷屏。

### 4.2 按钮布局
1. 模型选择键盘采用两列布局。
2. 会话列表采用单行单按钮的纵向列表，底部附带分页导航。
3. 会话详情固定保留“接管会话 / 查看历史 / 刷新详情 / 删除会话 / 返回列表”操作顺序。

## 5. 可读性与移动端适配
1. 单条 Telegram 消息以 3500 字符为安全阈值，超出时必须截断或切分。
2. 长结果优先保留末尾核心内容或输出摘要，避免整段原样转发导致移动端阅读困难。
3. 会话列表单页最多展示 6 条会话，适配手机端纵向浏览。
4. 工具输出需优先转为摘要，而不是把终端原始长日志完整推送给用户。

## 6. 已实现交互规范映射
1. `/start`、`/help`：展示命令入口说明。
2. `/whoami`：展示 chat_id、user_id、username。
3. `/model`：以按钮方式切换模型。
4. `/sessions`：进入会话管理菜单。
5. 普通文本消息：直接进入 AI 对话流程。
6. 过程日志优先使用 HTML 引用块显示；若 Telegram 解析失败，则自动回退为纯文本。

## 7. 一致性要求
1. 所有新增 Telegram 指令必须补充到帮助文案和设计文档。
2. 所有新增按钮回调必须说明触发结果、权限要求和失败反馈。
3. 若调整流式输出节奏、分页规则、截断规则或状态图标，必须同步更新本文件。
